<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.association.mapper.clubmapper">
<select id="getallclub" resultType="com.association.emtity.club">
    select * from club
</select>
    <select id="getclubbyid" resultType="com.association.emtity.club" parameterType="int">
    select * from club where clubID=#{id}
</select>
    <select id="getclubbyname" resultType="com.association.emtity.club" parameterType="String">
    select * from club where clubName=#{name}
</select>
    <select id="getclubbyAdmin" resultType="com.association.emtity.club" parameterType="String">
    select * from club where clubAdmin=#{admin}
</select>

</mapper>